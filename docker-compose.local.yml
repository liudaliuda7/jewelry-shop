services:
  jewelry-shop:
    image: node:20-alpine
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    stdin_open: true
    tty: true
    command: ["sh", "-c", "npm install && npm run dev"]
